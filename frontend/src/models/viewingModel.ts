export interface IViewing {
    userId: string,
    listingId: string,
    landlordId: string,
    viewingDate: String,
    viewingHour: String,
    status: string,
    createdAt: Date,
    updatedAt: Date,
}